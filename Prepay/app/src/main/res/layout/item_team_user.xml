<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="80dp">

    <TextView
        android:id="@+id/team_user_name"
        android:layout_width="73dp"
        android:layout_height="28dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="32dp"
        android:text="김싸피"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/manage_btn"
        android:layout_width="90dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:background="@drawable/round_background"
        android:text="관리자 설정"
        android:textColor="#0066CC"
        android:textSize="11dp"
        app:backgroundTint="@null"
        app:layout_constraintStart_toEndOf="@+id/team_user_name"
        app:layout_constraintTop_toTopOf="@+id/team_user_name" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/resign_btn"
        android:layout_width="70dp"
        android:layout_height="40dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="32dp"
        android:background="#F44336"
        android:text="강퇴"
        android:textColor="@color/white"
        android:textSize="11dp"
        app:backgroundTint="@null"
        app:layout_constraintStart_toEndOf="@+id/manage_btn"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>